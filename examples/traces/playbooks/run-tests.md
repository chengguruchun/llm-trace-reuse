# run tests

Same request (跑一下测试 / run the tests) — skip planning.

1. Use the project's usual **filtered** command, not a full unbounded suite.
2. Give the runner enough time (cold start is slow).
3. Reply with the filter, pass/fail counts, and duration.
4. A green compile is not a test.

Avoid: saying "should be fine" without numbers.
