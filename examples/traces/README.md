# Sample traces

Synthetic episodes and playbooks so `route.py` / `eval-loo.py` have something to read in this repository.

Copy `preferences.md` and `playbooks/` into **your** `$PATHBOOK_HOME` (default `.pathbook/`) and edit them.

Do not commit real traces. `append-episode.py` writes to `.pathbook/`, which is gitignored.
